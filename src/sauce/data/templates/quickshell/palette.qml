pragma Singleton
import QtQuick
// Written by themr switch — do not edit by hand
QtObject {
    readonly property color background:      "{{bg}}"
    readonly property color surface:         "{{bg1}}"
    readonly property color overlay:         "{{bg2}}"
    readonly property color widgetContainer: "{{bg1}}"
    readonly property color frameBackground: "{{bg2}}"
    readonly property color text:            "{{fg}}"
    readonly property color subtext:         "{{fg1}}"
    readonly property color mint:            "{{green}}"
    readonly property color rose:            "{{pink}}"
    readonly property color peach:           "{{orange}}"
    readonly property color sky:             "{{cyan}}"
    readonly property color lavender:        "{{purple}}"
    readonly property color aqua:            "{{teal}}"
    readonly property color sun:             "{{yellow}}"
    readonly property color error:           "{{error}}"
    readonly property color blue:            "{{blue}}"
}
